# Deny unwrap in contract code — use expect() with a message or proper error handling
disallowed-methods = [
    { path = "core::option::Option::unwrap", reason = "use expect() with a descriptive message instead" },
    { path = "core::result::Result::unwrap", reason = "use expect() with a descriptive message instead" },
]
